export const MIN_CLIP_DURATION = 0.5;
export const PHOTO_DEFAULT_DURATION = 3;
export const PHOTO_MAX_DURATION = 30;
export const OVERLAY_DEFAULT_DURATION = 3;
export const MUSIC_FADE_OUT = 2;

/** One frame at 30 fps. Keeps an overlay's start strictly inside its anchor clip. */
export const FRAME = 1 / 30;
